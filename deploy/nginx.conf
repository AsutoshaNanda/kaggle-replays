server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: route unknown paths back to index.html so client-side
    # routing (React Router) works on refresh / deep links.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
